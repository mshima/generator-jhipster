name: "Pull Request Labeler"
on:
    schedule:
        # * is a special character in YAML so you have to quote this string
        - cron:  '*/5 * * * *'

jobs:
    triage:
        runs-on: ubuntu-latest
        steps:
            - uses: mshima/labeler@master
              with:
                  repo-token: "${{ secrets.GITHUB_TOKEN }}"
