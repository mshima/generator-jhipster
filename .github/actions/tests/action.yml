#
# Copyright 2013-2021 the original author or authors from the JHipster project.
#
# This file is part of the JHipster project, see https://www.jhipster.tech/
# for more information.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: 'Run JHipster app tests'
description: 'Run JHipster app tests'
inputs:
  application-directory:
    description: 'Application path'
    required: false
  backend:
    description: 'Run backend tests'
    required: false
    default: 'true'
  frontend:
    description: 'Run frontend tests'
    required: false
    default: 'true'
  e2e:
    description: 'Run E2E tests'
    required: false
    default: 'true'
outputs:
  backend-outcome:
    description: 'Backend outcome'
    value: ${{ steps.backend.outcome }}
  frontend-outcome:
    description: 'E2E outcome'
    value: ${{ steps.frontend.outcome }}
  e2e-outcome:
    description: 'E2E outcome'
    value: ${{ steps.e2e.outcome }}
runs:
  using: 'composite'
  steps:
    - name: 'TESTS: Start docker-compose containers for e2e and backend tests tests'
      run: test "$JHI_TESTCONTAINERS" == "true" || npm run ci:e2e:prepare
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'TESTS: backend'
      id: backend
      run: test ${{inputs.backend}} != true || npm run ci:backend:test
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'TESTS: frontend'
      id: frontend
      run: test ${{inputs.frontend}} != true || npm run ci:frontend:test
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'TESTS: packaging'
      run: test ${{inputs.e2e}} != true || npm run ci:e2e:package
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'TESTS: Start docker-compose containers for e2e tests'
      run: test ${{inputs.e2e}} != true || test "$JHI_TESTCONTAINERS" != "true" || npm run ci:e2e:prepare
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'E2E: Run'
      id: e2e
      run: test ${{inputs.e2e}} != true || npm run ci:e2e:run
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
    - name: 'E2E: Teardown'
      run: test ${{inputs.e2e}} != true || npm run ci:e2e:teardown
      shell: bash
      working-directory: ${{ inputs.application-directory || env.JHI_FOLDER_APP }}
